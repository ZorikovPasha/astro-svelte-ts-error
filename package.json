{
  "name": "junica-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "prepare": "npx husky install",
    "format": "prettier --write  --plugin prettier-plugin-svelte .",
    "ts-check": "astro check && tsc --noEmit && svelte-check"
  },
  "dependencies": {
    "@astrojs/check": "^0.2.0",
    "@astrojs/node": "^6.0.0",
    "@astrojs/svelte": "^4.0.2",
    "astro": "^3.0.11",
    "svelte": "^4.2.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@astrojs/ts-plugin": "^1.1.3",
    "lint-staged": "^13.2.1",
    "svelte-check": "^3.5.1"
  }
}
